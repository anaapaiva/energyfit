<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <title>Recuperação de Senha</title>

    <style>
      body {
        margin: 0;
        padding: 0;
        font-family: Arial, Helvetica, sans-serif;
        background: #ffffff;
        color: #000000;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
      }

      .container {
        color: black;
        background-color: #e4e6e8;
        padding: 30px;
        border-radius: 10px;
        width: 380px;
        box-shadow: 0 0 20px rgba(0, 0, 0, 0.4);
        text-align: center;
      }

      h1 {
        margin-bottom: 20px;
      }

      label {
        font-size: 14px;
      }

      input[type="email"] {
        width: 100%;
        padding: 10px;
        margin-top: 8px;
        border-radius: 6px;
        border: none;
        background: #bcbfc3;
        color: #000; /* corrigido, porque estava branco em fundo cinza */
      }

      button {
        margin-top: 18px;
        width: 100%;
        padding: 10px;
        border: none;
        border-radius: 6px;
        background: #000000;
        color: #ffffff;
        font-weight: bold;
        cursor: pointer;
        font-size: 16px;
      }

      button:hover {
        background: #333333;
      }

      .login-link {
        display: block;
        margin-top: 15px;
        color: #000000;
        text-decoration: none;
      }

      .login-link:hover {
        text-decoration: underline;
      }

      .msg-error {
        color: red;
        font-size: 14px;
        margin-top: 10px;
      }

      .msg-success {
        color: green;
        font-size: 14px;
        margin-top: 10px;
      }
    </style>
  </head>

  <body>
    <div class="container">
      <h1>Recuperar Senha</h1>

      <!-- Exibir mensagens vindas do backend -->
      <% if (typeof error !== "undefined") { %>
        <p class="msg-error"><%= error %></p>
      <% } %>

      <% if (typeof success !== "undefined") { %>
        <p class="msg-success"><%= success %></p>
      <% } %>

      <form action="/api/v1/auth/recuperar-senha" method="POST">
        <label for="email">Digite seu e-mail cadastrado:</label><br />
        <input type="email" id="email" name="email" required /><br />

        <button type="submit">Enviar link de recuperação</button>
      </form>

      <a href="/login" class="login-link">Voltar para o Login</a>
    </div>
  </body>
</html>
